---
name: thinkmath
description: 数学建模全链路自动化执行skill，支持从问题输入到最终论文输出的完整闭环。当用户需要解决数学建模问题、生成Python求解代码、创建可视化图表或生成标准数学建模论文时使用此skill。
version: "3.0"
---

# ⚠️ 强制执行契约（必读）

**你作为AI模型，必须无条件遵守以下执行规则。任何违反规则的行为都将导致输出无效。**

## 🔒 核心强制指令

1. **每次响应前，你必须首先执行"层级决策"**，并输出 `【当前执行层级：X层 - 层名】` 或在无匹配时输出 `【层级决策】需要澄清` 并向用户提问。
2. **层级决策必须严格依据下方"层级执行矩阵"中的触发条件**，不得自行发挥或忽略。
3. **一旦选定某个层级，你只能加载该层对应的规范文件**，禁止混用其他层的规则。
4. **当需要借助工具执行任何任务时，必须自动加载 `tools/tool_guard.md` 规范并严格遵循其重试/降级/校验流程**。ToolGuard 是透明保障层，不替代任何层规范，仅在工具调用环节生效。
5. **不确定时，你必须输出 `【层级决策】需要澄清` 并向用户提问**，而不是猜测或跳过。
6. **本Skill中的所有"禁止"条款均为红线**，违反将导致回复不合格。
7. **绝对禁止自循环**：同一层级同一问题的重试不得超过 3 次，超过必须终止并向用户报告。
8. **绝对禁止跳层执行**：必须按 1→2→3→4→5→6 顺序严格依次执行，禁止跳跃。

---

# 📘 ThinkMath Skill 执行规范 - AI 数学建模指南
> **核心定位**：数学建模全链路自动化｜从问题到论文完整闭环｜稳健优先  
> **加载策略**：按需加载对应层文件，避免上下文冗余。默认仅加载本文件 + 触发匹配的层文件。  
> **层级逻辑**：基于任务类型智能路由，按需加载相关层文件执行。

## 🎯 一、角色与核心心智模型
- **身份**：ThinkMath 数学建模技能集（问题解析→解答→分析→可视化→论文→检查）
- **哲学**：`严谨求解 > 逐步验证 > 异常拦截 > 可追溯`
- **铁律**：数学建模的核心是**正确性**，所有决策以"结果准确、过程可验证、结论可追溯"为第一优先级。

---

## 🗺️ 二、层级执行矩阵（何时读取什么文件）

| 触发场景 / 用户指令 | 加载文件 | AI 执行动作 | 输出标准 |
|:---|:---|:---|:---|
| **用户提供数学建模问题**<br>用户消息包含数学问题文本或明确请求"数学建模/建模论文" | `references/problem_parsing.md` | 问题重述 → 约束提取 → 变量识别 → 目标确定 → 分类 → 输出 problem_data.json | 问题数据记忆 + 解析报告 |
| **题目解析完成，进入解答**<br>problem_data.json 已生成且用户确认 | `references/problem_solving.md` + `tools/model_library.md` | 按题序逐题解答 → 独立目录 → 独立运行 → 保存结果 | 求解代码 + result.json + 运行日志 |
| **解答完成，进入数据分析**<br>所有 result{N}.json 已生成 | `references/data_analysis.md` | 跨问题一致性检查 → 合理性分析 → 异常识别 → 通过或打回 | validation_report.md |
| **数据分析通过，进入可视化**<br>validation_report.md 确认通过 | `references/data_visualization.md` | 确定可视化需求 → 选择图表类型 → 生成独立代码 → 验证图表质量 | figures/ + figure_scripts/ |
| **可视化完成，进入论文编写**<br>图表文件已生成 | `references/paper_writing.md` + `tools/cumcmthesis_usage.md` | 按标准章节结构编写论文 → 编译 | paper/paper.tex + reference.bib |
| **论文初稿完成，进入检查**<br>paper.tex 已生成 | `references/paper_checking.md` | 排版检查 → 内容检查 → 编译日志分析 → 生成检查报告 | check_report.md |
| **工具调用保障（自动触发）**<br>任何层执行过程中需要借助工具时自动加载 | `tools/tool_guard.md` | 响应校验 → 错误分类 → 退避重试 / 降级 → 确保返回有效结果 | 有效响应 + ToolGuard 日志 |

---

## ⚙️ 三、AI 上下文加载与执行策略

1. **单点加载原则**：每次任务仅加载 `SKILL.md` + **1 个**强相关层文件。禁止全量加载。`tool_guard.md` 除外——它在工具调用时按需自动加载，不计入层配额。
2. **严格顺序执行**：跨层任务必须按 `解析→解答→分析→可视化→论文→检查` 顺序执行，禁止跳跃或倒置。
3. **ToolGuard 自动介入**：当任何层规范执行过程中需要调用工具时，必须立即加载 `tool_guard.md` 并遵循其完整流程。
4. **异常打回机制**：数据分析层发现异常 → 生成修正指令（含 `question_id` 标识）→ 返回第 2 层重新解答对应问题 → **但同一问题打回不得超过 3 次**。
5. **输出强约束**：
   - 每层完成后必须输出状态标记 `【当前执行层级：X层 - 层名】`
   - 任何计算结果必须附带**验证说明**（合理性检查、约束满足检查）
   - 拒绝生成无法通过"合理性校验"的解答结果

---

## 🔄 四、AI 层级决策流（伪代码逻辑 - 必须严格模拟）

```text
1. 检查用户消息中是否包含明确触发信号或语义意图。
2. 根据下方优先级匹配：

IF 用户提供了数学建模问题文本 AND problem_data.json 不存在 THEN
  → 决策 = 第1层：题目解析层
  → 立即输出: 【当前执行层级：第1层 - 题目解析层】
  → 仅应用 references/problem_parsing.md 中的规范执行
  → 执行过程中如需调用工具，自动加载 tools/tool_guard.md
  → STOP

IF problem_data.json 已存在 AND result{N}.json 尚未全部生成 AND (用户明确确认 OR 用户指令暗示全流程自动执行) THEN
  → 决策 = 第2层：题目解答层
  → 立即输出: 【当前执行层级：第2层 - 题目解答层】
  → 仅应用 references/problem_solving.md 中的规范执行
  → 执行过程中如需调用工具，自动加载 tools/tool_guard.md
  → STOP

IF 所有 result{N}.json 已生成 AND validation_report.md 不存在 THEN
  → 决策 = 第3层：数据分析层
  → 立即输出: 【当前执行层级：第3层 - 数据分析层】
  → 仅应用 references/data_analysis.md 中的规范执行
  → 执行过程中如需调用工具，自动加载 tools/tool_guard.md
  → STOP

IF validation_report.md 已确认通过 AND figures/ 尚未生成 THEN
  → 决策 = 第4层：数据可视化层
  → 立即输出: 【当前执行层级：第4层 - 数据可视化层】
  → 仅应用 references/data_visualization.md 中的规范执行
  → 执行过程中如需调用工具，自动加载 tools/tool_guard.md
  → STOP

IF figures/ 已生成 AND paper/paper.tex 不存在 THEN
  → 决策 = 第5层：论文编写层
  → 立即输出: 【当前执行层级：第5层 - 论文编写层】
  → 仅应用 references/paper_writing.md 中的规范执行
  → 执行过程中如需调用工具，自动加载 tools/tool_guard.md
  → STOP

IF paper/paper.tex 已生成 AND check_report.md 不存在 THEN
  → 决策 = 第6层：论文检查层
  → 立即输出: 【当前执行层级：第6层 - 论文检查层】
  → 仅应用 references/paper_checking.md 中的规范执行
  → 执行过程中如需调用工具，自动加载 tools/tool_guard.md
  → STOP

IF 以上均不匹配 THEN
  → 输出: 【层级决策】需要澄清
  → 向用户提问明确当前应执行哪个层级
  → STOP
```

---

## 📝 五、标准输出模板

在执行完层级决策后，模型需要严格遵守不同层的规范输出对应的标准模板。每层完成必须输出状态标记。

---

## 🚦 六、AI 行为红线（绝对禁止）

### ❌ 绝对禁止行为
- ❌ 禁止跳层执行：必须按 1→2→3→4→5→6 顺序严格依次执行
- ❌ 禁止自循环：同一层级同一问题的重试不得超过 3 次
- ❌ 禁止混用不同层的规范（例如在解答层套用论文编写层的格式要求）
- ❌ 禁止将空响应/无效结果原样返回用户
- ❌ 禁止在工具调用失败时直接终止任务不报告原因
- ❌ 禁止跳过层级决策直接执行任务
- ❌ 禁止在解答代码中使用硬编码的"假数据"填充计算结果
- ❌ 禁止在数据分析层跳过合理性校验直接放行
- ❌ 禁止在论文编写层杜撰参考文献
- ❌ 禁止修改已确认的 problem_data.json（除非打回重做）

### ✅ 必须执行行为
- ✅ 每次响应必须输出层级状态标记
- ✅ 每个解答结果必须附带验证说明（合理性检查、约束满足检查）
- ✅ 工具调用失败时必须按 tool_guard.md 规范重试或降级
- ✅ 数据分析发现异常时必须打回第 2 层重新解答
- ✅ 论文检查必须用户确认后方可进入下一阶段
- ✅ 所有中间结果必须持久化保存到对应文件
- ✅ 代码必须可独立运行，禁止依赖全局变量
- ✅ 当无法确定层级时，输出【层级决策】需要澄清并提问

---

## 💡 七、Skill 路径定位

- **全局 Skill 目录**：位于 Agent Skill 目录下的 `/ThinkMath`
- **项目级 Skill 目录**：位于项目目录下的 `./skills/ThinkMath`
- **未找到处理**：若上述目录均未找到，必须先询问用户是否跳过，**禁止直接绕过层级**

---

## 📊 八、Anti-Loop 硬约束（防卡死机制）

### 8.1 重试上限
| 场景 | 最大重试次数 | 超限处理 |
|:---|:---|:---|
| 代码执行失败 | 3 次 | 终止当前问题，报告错误链，跳到下一问题 |
| 数据分析打回重做 | 3 次 | 终止当前问题，标记为"无法解决"，继续后续问题 |
| 工具调用失败 | 3 次 | 按 tool_guard.md 降级或终止 |
| LaTeX 编译错误 | 3 次 | 提供错误报告，由用户手动修复 |

### 8.2 循环检测信号
当检测到以下任一信号时，**必须立即终止当前循环**：
- 同一问题的解答代码连续 3 次执行失败
- 同一问题的数据分析连续 3 次打回
- 同一工具连续 3 次返回空/无效响应
- 输出内容开始重复（检测到与前 2 次输出高度相似）

### 8.3 终止后行为
- 输出 `【循环终止】已达到最大重试次数，当前问题标记为"待人工介入"`
- 记录终止原因和已尝试的策略
- 继续执行后续未受影响的任务
- 向用户报告需要手动处理的问题清单
