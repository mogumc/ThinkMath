---
name: thinkmath
description: 数学建模全链路自动化执行skill，支持从问题输入到最终论文输出的完整闭环。当用户需要解决数学建模问题、生成Python求解代码、创建可视化图表或生成标准数学建模论文时使用此skill。
---

# ThinkMath Skill 执行规范

## 层级定义
当用户输入数学建模问题后，系统按以下六个层级顺序执行：

### 工具保障层
> 额外层级 | 不参与六个层级执行，在必要时介入保障任务链
- **执行条件**：使用agent工具（如task、web_fetch、search_content等）时触发
- **必须读取**：`tools/tool_guard.md`
- **执行指令**：执行前准备、响应校验、失败分类与处理、重试与降级机制
- **执行规范**：输出"【工具保障层介入】"

### 1. 题目解析层
- **必须读取**：`references/problem_parsing.md`
- **执行指令**：接收问题文本，提取核心约束、变量定义和目标函数，识别问题类型，创建题目数据记忆
- **执行规范**：输出"【当前执行层级：题目解析层】"

### 2. 题目解答层
- **必须读取**：`references/problem_solving.md` + `tools/model_library.md`
- **执行指令**：按题目顺序逐题逐问解答，为每个问题创建独立的工作目录和代码文件
- **执行规范**：输出"【当前执行层级：题目解答层】"

### 3. 数据分析层
- **必须读取**：`references/data_analysis.md`
- **执行指令**：收集解答结果，检查数据一致性，异常数据打回题目解答层重做
- **执行规范**：输出"【当前执行层级：数据分析层】"

### 4. 数据可视化层
- **必须读取**：`references/data_visualization.md`
- **执行指令**：根据验证后的数据生成适当图表，生图代码与数据处理解耦
- **执行规范**：输出"【当前执行层级：数据可视化层】"

### 5. 论文编写层
- **必须读取**：`references/paper_writing.md` + `tools/cumcmthesis_usage.md`
- **执行指令**：使用cumcmthesis模板编写论文，整合所有解题结果、数据分析和图表
- **执行规范**：输出"【当前执行层级：论文编写层】"

### 6. 论文检查层
- **必须读取**：`references/paper_checking.md`
- **执行指令**：检查论文排版，提供编译指导，用户手动确认PDF和日志
- **执行规范**：输出"【当前执行层级：论文检查层】"

## 执行规范
1. **读取层级文件**：按上方层级定义读取对应文件
2. **执行层级任务**：按照层级规范执行具体任务
3. **输出执行状态**：输出当前执行的层级名称

## 核心设计原则
1. **独立性原则**：每个问题独立解答，禁止串题
2. **层级隔离原则**：每个层级读取对应规范，禁止串层
3. **质量控制原则**：数据异常打回重做，论文检查用户确认